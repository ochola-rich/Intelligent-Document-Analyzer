services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: ida-postgres
    environment:
      POSTGRES_DB: filesanalyzer_db
      POSTGRES_USER: blaq
      POSTGRES_PASSWORD: Ochola@254
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init-pgvector.sql:/docker-entrypoint-initdb.d/init-pgvector.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U blaq -d filesanalyzer_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  java-analytics:
    build:
      context: ./filesanalyzer
    container_name: ida-java-analytics
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      SERVER_PORT: 8082
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/filesanalyzer_db
      SPRING_DATASOURCE_USERNAME: blaq
      SPRING_DATASOURCE_PASSWORD: Ochola@254
    ports:
      - "8082:8082"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8082/health"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 25s

  ml-service:
    build:
      context: ./ml-service
    container_name: ida-ml-service
    environment:
      DATABASE_URL: postgresql://blaq:Ochola%40254@postgres:5432/filesanalyzer_db
    ports:
      - "8081:8081"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8081/health')"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 15s

  api-gateway:
    build:
      context: ./Api-Gateway
    container_name: ida-api-gateway
    depends_on:
      ml-service:
        condition: service_healthy
    environment:
      WORD_SERVICE_URL: http://ml-service:8081/upload
      PDF_SERVICE_URL: http://ml-service:8081/upload
      SEARCH_SERVICE_URL: http://ml-service:8081/search
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080/health"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 10s

  frontend:
    build:
      context: ./frontend
    container_name: ida-frontend
    environment:
      VITE_API_BASE: http://localhost:8080
    ports:
      - "5173:5173"
    depends_on:
      api-gateway:
        condition: service_healthy

volumes:
  postgres_data:
